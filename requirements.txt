# Phase 0 Step 0.3 — Python analysis stack (install in a venv)
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0
sentence-transformers>=2.2.0
